name: Publish rubykatzen.com env
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - targets/hawkeye.yml
      - keys/hawkeye.pub
jobs:
  publish:
    uses: rubykatzen/flightdeck/.github/workflows/publish-sops-env-shared.yml@main
    with:
      manifest: targets/hawkeye.yml
    secrets: inherit
