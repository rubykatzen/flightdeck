name: Telegram Release Notify
on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:
jobs:
  notify:
    permissions:
      contents: read
      actions: read
    uses: rubykatzen/releaser/.github/workflows/telegram-release-notify-shared.yml@v0.5
    secrets: inherit
